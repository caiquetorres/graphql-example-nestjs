/**
 * Enum that represents all the keys used in this module to represent
 * objects that will be injected
 */
export enum ModuleConstant {
  GOOGLE_SIGN_IN_MODULE_OPTIONS = '__google_sign_in_module_options__',
}
